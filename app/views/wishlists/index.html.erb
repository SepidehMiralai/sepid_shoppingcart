<p id="notice"><%= notice %></p>

<h1><%=@current_user.name%>'s Wishlist</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @wishlists.each do |wishlist| %>
      <tr>
        <td><%= wishlist.name %> </td>
        <td><%= wishlist.price %> </td>
        <td><%= image_tag(wishlist.img_url, alt: 'product Image', class: 'list_image') %></td>
        <td><%= link_to 'Delete', wishlist, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>      
    <% end %>
  </tbody>
</table>

<br>


